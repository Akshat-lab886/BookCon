# Build outputs
*.apk
*.apk.idsig
build/
.gradle-home/
build-tools-35.zip

# Project artefacts that shouldn't be in source control
.DS_Store
android/.DS_Store
android/app/.DS_Store
android/app/bookcon-release.jks
server/.DS_Store
.android-sdk/
.buildhome/
BookCon-*.apk.idsig

# Gradle caches & local daemon (not source)
.gradle/
.kotlin/
*.class
*.jar
*.tmp

# Session scratch
dsh-session-*
SESSION_CONTEXT_RECOVERY.md

# Local Gradle / Android SDK overrides (not part of the build contract)
.gradle-runtime/
.android-runtime/
